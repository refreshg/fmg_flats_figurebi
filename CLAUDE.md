# CLAUDE.md — ვერტიკალი

პროექტის კონტექსტი, ტერმინოლოგია და გადაწყვეტილებები.
**წაიკითხე ეს ფაილი კოდის წერამდე.**

---

## რა არის პროექტი

Odoo 19 Enterprise-ის მოდული უძრავი ქონების დეველოპერისთვის.
**შიდა ინსტრუმენტია სეილს მენეჯერისთვის** — არა საჯარო ვიჯეტი მყიდველისთვის.

ბინები ინახება Odoo-ს სტანდარტულ პროდუქტებად, ვიზუალური ამომრჩევი (ფასადი →
სართული → ბინა) აშენებულია OWL-ით, გაყიდვა კი მიდის სტანდარტული
opportunity → შეთავაზება → ორდერი ჯაჭვით.

---

## ტერმინოლოგია

| ქართული | English | კოდში |
|---|---|---|
| ბინა / ერთეული | unit | `product.template` + `vk_is_unit` |
| გენგეგმა | masterplan | `vertikali.view` type=`masterplan` |
| ფასადი | facade | `vertikali.view` type=`facade` |
| სართულის ზედხედი | floor plan | `vertikali.view` type=`floor` |
| შახმატკა | grid / shakhmatka | selector-ის `grid` რეჟიმი |
| ზონა / კონტური | zone / polygon | `vertikali.polygon` |
| ჯავშანი | reservation | `sale.order` state=`draft`/`sent` |

---

## გადაწყვეტილებები და მიზეზები

### D1 — Odoo მოდული, არა SaaS
თავდაპირველი PRD multi-tenant SaaS-ს აღწერდა (React, tRPC). **2026-08-23-ს
[research/prd.md](research/prd.md) გადაიწერა (v2.0)** და ახლა რეალურ
პროდუქტს აღწერს — Odoo მოდულს. SaaS-ის დეტალები git-ის ისტორიაშია.

### D8 — ნაშთი არის ორმაგი გაყიდვის კარიბჭე
თითო ბინა = 1 ცალი WH/Stock-ზე (`action_vk_init_stock` თესავს; Units
სიის Action მენიუშიც დევს ახალი ბინებისთვის). რეზერვაცია = ორდერის
დადასტურება → sale_stock რეზერვავს quant-ს; გაყიდვა = მიწოდების
ვალიდაცია → ჩამოწერა; გაუქმება = რეზერვის მოხსნა. **მაგრამ** Odoo
დადასტურებას ნაშთის გარეშეც უშვებს (მიწოდება უბრალოდ იცდის) — ამიტომ
რეალური კარიბჭე `action_confirm`-ის override-ია: `free_qty < qty` →
UserError ბინის კოდით.

**vk_state-ის სემანტიკა შეიცვალა (2026-08-24, მომხმარებლის გადაწყვეტილება):**
draft/sent შეთავაზება ბინას **აღარ** აკავებს — რამდენიმე ლიდს შეუძლია
პარალელური შეთავაზება ერთ ბინაზე. `reserved` = დადასტურებული ორდერი,
`sold` = ვალიდირებული მიწოდება (`qty_delivered > 0`).

`sale.order.vk_stage` — ბინის გარიგების საკუთარი მილსადენი:
reservation → contract (ხელით ღილაკი) → sold / cancelled. Estate →
Reservations მენიუ (kanban ჯგუფდება ეტაპებით, list ფერებით).

**დამატებები (2026-08-24, v3.3–3.5):**
- გაუქმება **ყოველთვის** აბრუნებს ნაშთს — done მიწოდებას ავტომატური
  return transfer უკეთდება (`action_cancel` override)
- reserved/sold ბინის მიბმა **იკრძალება** ლიდზეც და შეთავაზების ხაზზეც
  (სერვერის guard-ები + ბარათზე ღილაკების დამალვა შენიშვნით)
- pick-რეჟიმი generic-ია: სამიზნე ლიდი **ან** draft შეთავაზება
  (`vk_pick_model/vk_pick_id`); ბარათიდან Attach ორდერზე ხაზს ქმნის
- deep-link (`Show on Plan`) პროექტის ჩართულ საფეხურებს ემორჩილება:
  floor → facade → masterplan → grid; მონიშნულის ბარათი ყოველ ვიუზე
  თავიდან იხსნება (დახურვა მხოლოდ მიმდინარე ეკრანზეა); pick-ში — არა
- ფილტრები ხუთივე ვიუზეა: floor-ზონა/პინი ქრება+ითიშება, ფასადის
  tooltip/პანელი გაფილტრულ ციფრებს აჩვენებს (Found: X / Y), Grid+
  აქრობს კი არა, აფერმკრთალებს; Properties — 100-იანი ფეიჯინგი
- „Number floors…" toolbar-იდან მოხსნილია (autoNumberZones JS-ში დარჩა)

### D7 — კონფიგურირებადი ატრიბუტები `vertikali.option`-ით
Condition და View **Selection-ებად ვერ დარჩა** — გუნდი თავად ამატებს
მნიშვნელობებს (რემონტის პაკეტები, ხედები პროექტზეა დამოკიდებული), Selection
კი დეველოპერის გარეშე ვერ იზრდება. ერთი პატარა მოდელი `attribute` სვეტით
ფარავს ორივეს; დამატება dropdown-ის quick-create-ითაც შეიძლება.
Orientation Selection-ად დარჩა — კომპასის 8 მიმართულება ფიზიკაა, არა
ტაქსონომია; vk_rooms-იც დარჩა, რადგან კოდები (`2+1`) ლოგიკაშია ჩაქსოვილი.

### D2 — შიდა ინსტრუმენტი
სეილს მენეჯერისთვის. `website` დაინსტალირებულია, მაგრამ **V1-ში არ
გამოიყენება** — ეს შეგნებული გადაწყვეტილებაა, არა გამორჩენა.

### D3 — თითო ბინა = ცალკე `product.template`
განიხილებოდა ალტერნატივა: ერთი პროდუქტი + `stock.lot` (serial) თითო ბინაზე.
**უარვყავით, რადგან ფასი.** `list_price` პროდუქტზეა, არა serial-ზე, ხოლო
თითოეულ ბინას თავისი ფასი აქვს. lot-ზე ინდივიდუალური ფასი custom კოდს
მოითხოვდა — ზუსტად იმას, რის თავიდან აცილებაც გვინდოდა.
126 პროდუქტი Odoo-სთვის ნორმალურია.

### D4 — Standard-first
custom კოდი მხოლოდ მაშინ, როცა **დადასტურდება** რომ სტანდარტი ვერ ფარავს.
დადასტურება = კონკრეტული მოდელი/ველი შემოწმებულია live ინსტანსზე.

### D5 — ჯერ ვიზუალი, მერე ბექენდი
პროტოტიპები [prototype/](prototype/)-ში დამტკიცდა, მერე გადმოვიდა Odoo-ში.

### D6 — გაყიდვის ჯაჭვი
`crm.lead` → რამდენიმე `sale.order` → ჯავშანი → გაყიდვა.
`crm.lead.order_ids` **სტანდარტულია** — custom კოდი არ დასჭირვებია.

---

## რას ვიყენებთ სტანდარტიდან

გადამოწმებულია live ინსტანსზე JSON-2 API-ით:

| საჭიროება | სტანდარტული ველი |
|---|---|
| ბინა | `product.template`, `type='consu'`, `is_storable=True` |
| კოდი (A-0504) | `default_code` |
| ფასი | `list_price` |
| პროექტი → კორპუსი | `product.category.parent_id` (იერარქიულია) |
| ტეგები | `product_tag_ids` |
| გეგმის სურათი | `image_1920` |
| opportunity → შეთავაზებები | `crm.lead.order_ids` ↔ `sale.order.opportunity_id` |
| ჩატი, აქტივობები | `mail.thread`, `mail.activity.mixin` |

---

## Custom კოდი — და რატომ

| # | რა | რატომ ვერ ფარავს სტანდარტი |
|---|---|---|
| C1 | `vk_*` ველები | `product_properties` არსებობს, მაგრამ **computed არ დაიშვება** და group-by/sort შეზღუდულია |
| C2 | `vk_price_sqm` | computed `store=True`. Properties-ზე შეუძლებელია |
| C3 | `vk_state` | `qty_available` და `sales_count` **`store=False`** — ვერ სორტირდება, ვერ ფილტრდება |
| C4 | პოლიგონები | Odoo-ს არ აქვს clickable-overlay ვიჯეტი სურათზე |
| C5 | შახმატკა | `pivot` რიცხვებს აჩვენებს, არა ფერად სტატუს-ბადეს კლიკით |

**შენიშვნა (მოძველდა 2026-08-23):** თავიდან ვგეგმავდით Properties-ს
მეორეხარისხოვანი ატრიბუტებისთვის (რემონტი, ხედი). რეალურად ისინი
`vertikali.option`-ზე გადავიდა (D7) — მომხმარებელს მნიშვნელობების
დამატება სჭირდებოდა და ფილტრებში ჩვენება, რასაც Properties ვერ იძლეოდა.

---

## მოდელები

### `product.template` (გაფართოება)
`vk_is_unit` `vk_building` `vk_section` `vk_floor` `vk_rooms`
`vk_orientation` `vk_view_id` `vk_area_total` `vk_area_living`
`vk_area_balcony` `vk_price_sqm` `vk_handover` `vk_state`
`vk_rooms_detail` `vk_condition_id` `vk_has_image` `vk_image_ids`
`vk_layout_id`

`vk_condition_id` / `vk_view_id` → `vertikali.option` (D7).
`vk_has_image` — computed store; სელექტორი ამით წყვეტს layout-ზე
გადავარდნას სურათის ჩამოტვირთვის გარეშე.

### `crm.lead` (გაფართოება)
`vk_unit_ids` (m2m), `vk_unit_count`, `vk_unit_codes` (kanban-ბარათზე).
ღილაკები: Choose Units / Show on Plan / Reserve. create/write guard —
დაკავებული ბინის ახალი მიბმა UserError-ია.

### `sale.order` / `sale.order.line` (გაფართოება)
`vk_stage`, `vk_contract_signed`, `vk_has_units`; ღილაკები: Choose Units,
Show on Plan, Contract Signed, Mark Sold. `action_confirm` — free_qty
guard; `action_cancel` — done მიწოდების return; ხაზის create — დაკავებული
ბინის guard. `stock.picking.button_validate` — vk_state refresh.

### `vertikali.option`
კონფიგურირებადი მნიშვნელობები (`attribute`: condition / view). ACL:
სეილსმენი კითხულობს **და ქმნის** (quick-create), მენეჯერი — ყველაფერს.
საწყისი Condition-ები `data/option_data.xml`-შია, `noupdate="1"`.

### `vertikali.layout`
განმეორებადი ბინის ტიპი: გეგმა ერთხელ იტვირთება და 80 ბინა იზიარებს.
ბინის საკუთარი `image_1920` ყოველთვის იმარჯვებს layout-ის გეგმაზე.

### `vertikali.unit.image`
დამატებითი სურათი ბინაზე **ან** layout-ზე (`product.image` არ არსებობს —
`website_sale` არ არის დაინსტალირებული).

### `vertikali.project`
რომელი საფეხურები აქვს პროექტს: `use_masterplan` / `use_facade` /
`use_floorplan` / `use_grid`.
**ყველა პროექტს ერთი გზა არ აქვს** — ზოგს ფასადი არ აქვს, ზოგს ზედხედი.
ვალიდაცია: მინიმუმ ერთი საფეხური ჩართული უნდა იყოს.

ასევე: `image` (ყდის რენდერი), `tagline`, `description`, `handover` —
პროექტის ბარათისთვის; `unit_count` / `available_count` / `price_from` —
computed სტატისტიკა.

### `vertikali.block`
კოშკი პროექტში (A, B…). `code`, `points` (კონტური გენგეგმაზე), `color`,
`floors`, `facade_view_id`, computed `unit_count` / `available_count`.
ბინებს უკავშირდება `vk_building`-ით.

**ბლოკები დინამიურია** — რამდენიც დაიხაზება, იმდენი იქნება. სელექტორიდან
იქმნება (`+ add`), წინასწარი კონფიგურაცია არ სჭირდება.

> **შენიშვნა:** ტექნიკურად ეს `vertikali.polygon`-ითაც შეიძლებოდა
> (`target_view_id` უკვე არსებობს). ცალკე მოდელი იმიტომ გვჭირდება, რომ
> ბლოკს **საკუთარი მონაცემები** აქვს — ბინების რაოდენობა, სართულები,
> ფერი გადამრთველზე. სუფთა ზონა ამას ვერ შეინახავს.

### `vertikali.view`
სურათი + ტიპი. **სართულის ზედხედს თითო სართულზე თითო ჩანაწერი სჭირდება.**
`action_generate_floor_views()` ქმნის მათ ბინებიდან და აბამს ფასადის ზონებს.

### `vertikali.polygon`
კონტური. წერტილები **normalized (0–1)**, JSON-ად.
ვალიდაცია: ≥3 წერტილი, ყველა 0–1 შუალედში, target ან unit — არა ორივე.

---

## მნიშვნელოვანი ტექნიკური აღმოჩენები

### `vk_state` — რატომ არა `@api.depends`
`product.product`-ს **არ აქვს** `sale_order_line_ids`, ხოლო
`sale.order.line.product_template_id` არის **`store=False`** related ველი.
ამიტომ dependency chain შეუძლებელია — გადათვლა ტრიგერდება
`sale.order.line`-ისა და `sale.order`-ის override-ებიდან.
`sale.order`-ის დონეზეც საჭიროა: order-ის state-ის ცვლილება ყოველთვის არ
იწვევს `write()`-ს თითო ხაზზე.

### ⚠️ SVG overlay — გეომეტრია (ყველაზე ძვირად დაჯდა)

**წესი: overlay-ის პოზიცია JS-ით არ გამოითვალო.**

სურათი და SVG ერთ **CSS grid უჯრაშია** (`.o_vk_frame`), `> * { grid-area: 1/1 }`.
ბრაუზერი თავად ინახავს მათ ერთმანეთზე. კლიკიც **იმავე frame-ის**
`getBoundingClientRect()`-იდან იზომება.

სურათს **`object-fit` არ აქვს** — თვითონ განსაზღვრავს ზომას, ანუ frame
ზუსტად ნახატი ფართობია, letterbox-ის კომპენსაცია არ სჭირდება.

**რატომ ასე:** თავდაპირველად `measure()` ითვლიდა კვადრატს და inline
style-ით ათავსებდა SVG-ს. ექვსი მცდელობა დასჭირდა — ყოველი გამოთვლა
layout-ს ეყრდნობა იმ მომენტში, როცა სრულდება, და თუ layout ჯერ არ
დასრულებულა (hint-ის ზოლი დაემატა, სურათი ჯერ არ ჩაიტვირთა), შედეგი
მცდარია. რეალურ ეკრანზე overlay-ის პროპორცია იყო **0.843**, სურათის კი
**1.100** — 56px წანაცვლება.

**დიაგნოსტიკის გაკვეთილი:** პრობლემა მაშინ გაირკვა, როცა **ბაზაში
ჩაწერილი კოორდინატები** წავიკითხე (`x = 0.13…0.32`, სურათის მარცხენა
მესამედი). ეს პირველი ნაბიჯი უნდა ყოფილიყო — ეკრანს ყურება და ვარაუდი
ხუთჯერ არასწორ დასკვნამდე მიმიყვანა.

### Copy down — ზოლის სისქე
ზოლის სისქე **არ არის** bounding box-ის სიმაღლე. დახრილ ზოლზე ეს
რამდენიმე სართულს უტოლდება. ასევე არასწორია შუა ხაზით „ზედა/ქვედა"
ნახევრებად დაყოფა — დახრილზე მარცხენა ზედა წერტილი მარჯვენა ქვედაზე დაბლაა.
**სწორია ვერტიკალური ჭრილი** 9 ადგილას + მედიანა.

### OWL — `t-if` ჯაჭვი
`t-elif` **პირდაპირ** წინა `t-if`-ს უნდა მოსდევდეს. თუ შუაში ჩვეულებრივი
ელემენტი ჩაჯდა, ჯაჭვი წყდება და **ყველა შემდეგი ბრენჩი ჩუმად ითიშება** —
შეცდომის გარეშე.

ეს ერთხელ უკვე მოხდა: toolbar ჩავსვი body-ის ბრენჩებს შორის და ფასადის
და ზედხედის ბრენჩები ვეღარასოდეს ჩაირთვებოდა (სცენა ცარიელი რჩებოდა).

შემოწმება: სტრუქტურის სკრიპტული დათვლა (`t-if` / `t-elif` თანმიმდევრობა
ერთ დონეზე), არა თვალით.

### ნავიგაციის თანმიმდევრობა
```
პროექტის ბარათი → გენგეგმა → ფასადი → სართული → ბინა
```
**ავტომატური „მალსახმობები" არ გვინდა.** ერთი პროექტი / ერთი ბლოკი რომ
ავტომატურად აგვერჩია, მომხმარებელი პირდაპირ ფასადზე ხვდებოდა და ვერ
ხვდებოდა სად იყო. ბლოკი ავტომატურად მხოლოდ მაშინ აირჩევა, თუ გენგეგმა
გამორთულია **და** ბლოკი მართლაც ერთია.

### ზონა უნდა მიება სართულს
დახაზული ზონა **თავისთავად არსად მიდის** — სჭირდება `floor` და
`target_view_id`. სელექტორში: **Number floors…** (ზემოდან ქვემოთ ნომრავს
და აბამს ზედხედს) ან ცალკე ველი „Opens floor".

### Odoo 19 view syntax
- `expand="0"` **აღარ არსებობს** `<group>`-ზე search view-ში
- `<group string="Group By">` search view-ში **საერთოდ არ დაიშვება** —
  `string` ატრიბუტი group-ზე RelaxNG-ს არღვევს; group-by ფილტრი პირდაპირ
  `<search>`-ში ჩასვი, wrapper-ის გარეშე
- `<search>`-ში ყველა `<field>` უნდა იყოს **filters-ამდე**
- ყველა იწვევს `RELAXNG_ERR` შეცდომას და **მთელი registry ვერ იტვირთება**
- ბირთვის `crm.lead.search` კარგი ნიმუშია

### SCSS — libsass-ის ხაფანგები
Odoo ბანდლებს **libsass**-ით აკომპილირებს, არა dart-sass-ით. `min(520px,
92vw)` libsass-ში Sass-ის ფუნქციაა და px/vw შერევაზე **მთელი backend-ის
CSS ვარდება** („A css error occured, using an old style"). გამოსავალი:
`clamp()` — ის Sass-ის ფუნქცია არ არის და CSS-ში უცვლელად გადის.
ლოკალური dart-sass შემოწმება ამას **ვერ დაიჭერს**.

### XML კომენტარში `--` აკრძალულია
`<!-- ესე -- არა -->` XML-ის შეცდომაა და დეპლოის ვალიდაცია ჭრის. commit-მდე
გაუშვი კომენტარების სკრიპტული შემოწმება (ორჯერ დაგვემართა).

### Asset ბანდლები დეპლოის შემდეგ ძველი რჩება
მოდულის განახლება ბანდლებს ავტომატურად **არ** აწყობს თავიდან — ბრაუზერი
საათობით ძველ JS/CSS-ს იღებს („განვაახლე და არაფერი შეიცვალა").
იძულებითი გადაწყობა: წაშალე `ir.attachment`-ები სახელით `assets_web%`
და გამოითხოვე `/web/assets/1/any/web.assets_web.min.js` — გადაწყობს.
გადამოწმება: გადმოწერე ბანდლი და `grep`-ით ნახე ახალი სიმბოლო შიგნითაა თუ არა.

### `/web/image` და base64
სურათები RPC-ში base64-ად **არ წამოიღო** — პროექტის ყდა 3.2MB იყო და
პირველი paint წამებით იბლოკებოდა. `/web/image/<model>/<id>/<field>/<WxH>`
+ `?unique=<write_date>` ქეშირებადია. **გაფრთხილება:** ანონიმური მოთხოვნა
403-ის ნაცვლად **placeholder-ს (6078 bytes) აბრუნებს 200-ით** — curl-ით
ტესტი შეცდომაში შეგიყვანს; ავტორიზებული ბრაუზერი რეალურ სურათს იღებს.

### Odoo list-ის მასობრივი რედაქტირება მხოლოდ ხილულ გვერდს ეხება
გვერდის checkbox-ით მონიშვნა + inline რედაქტირება მხოლოდ **ხილულ 80 ჩანაწერს**
შეეხო; დანარჩენი 46 გვერდზე დარჩა უცვლელი. „Select all N" ბმულით უნდა
აირჩეს სრული სია, ან API-ით გაკეთდეს.

### CSS: თანაბარ specificity-ზე გვიანდელი იგებს — სამჯერ დაგვარტყა
ერთი და იგივე დაავადება სამ ადგილას: (1) `.o_vk_zone`-ის ბაზური fill
ფარავდა სტატუსების ფერებს; (2) `.o_vk_ptable th` ფარავდა `.o_vk_num_c`-ის
ცენტრირებას; (3) `min(px,vw)` ცალკე ამბავია. წესი: როცა კლასი „არ ჭრის",
ჯერ specificity/თანმიმდევრობა შეამოწმე — გამოსავალი კომპოზიტური
სელექტორია (`.o_vk_zone.o_vk_uz_res`), არა `!important`.

### ერთი კლასის ორი ერთნაირი მეთოდი — Python ჩუმად ჩრდილავს
`SaleOrder`-ში ორი `action_cancel` აღმოჩნდა — Python ბოლოს იმახსოვრებს და
ახალი ლოგიკა მკვდარი კოდი იყო, შეცდომის გარეშე. ახალი override-ის წინ
`grep "def <name>"` გაუშვი იმავე ფაილზე.

### `matchesFilters`-ის ველები ყველა unit-წაკითხვაში უნდა იყოს
ფილტრი კლიენტზე ითვლის — თუ რომელიმე searchRead-ს ფილტრის ველი აკლია
(მაგ. `vk_orientation` loadZoneUnits-ში), ის სია მთლიანად „ჩამქრალი"
გამოდის, სხვა წყაროდან ნათვლილი ციფრები კი სწორია. ახალი ფილტრის
დამატებისას ველი ყველა სიაში ჩაამატე.

### Client action-ის params reload-ს ვერ იტანს
Breadcrumb-ით დაბრუნება/F5 client action-ს URL-იდან აღადგენს params-ის
გარეშე. სელექტორი კონტექსტს (focus/pick) და ნავიგაციას (project/mode/view)
sessionStorage-ში ინახავს (`vertikali.ctx`, `vertikali.nav`); „← უკან" და
„← Projects" ასუფთავებს.

### `noupdate="1"` წაშლილ ჩანაწერს ვერ იცავს
noupdate მხოლოდ **არსებულ** ჩანაწერს იფარავს გადაწერისგან. თუ მომხმარებელმა
ჩანაწერი წაშალა, მისი `ir.model.data` ბმაც ქრება და შემდეგი upgrade მას
**თავიდან ქმნის**. ასე ბრუნდებოდა ყოველ დეპლოიზე 21 demo-ზოლი, რომლებიც
გუნდმა წაშალა და თავისით ჩაანაცვლა. გამოსავალი: როცა demo რეალურ მონაცემად
იქცა, ფაილი manifest-ის `data`-დან **ამოიღე** — noupdate=1 ობოლი ჩანაწერები
upgrade-ზე არ იშლება, ამიტომ არსებული მონაცემი უცვლელი რჩება.
(`data/demo_building_a.xml` 2026-08-24-ს ამოღებულია; ფაილი რეპოში რჩება.)

### `base_import_module` არ ტვირთავს Python-ს
ატვირთული მოდული `imported: true`-ია და **მხოლოდ XML/CSV იტვირთება**.
უსაფრთხოების შეზღუდვაა. ამიტომ მოდული ფაილურად უნდა განთავსდეს
addons path-ში.

### odoo.conf-ის `logfile`
თუ `logfile` მითითებულია, ინსტალაციის output ფაილში მიდის და ტერმინალი
ცარიელი რჩება. deploy-ისას გამოიყენე **`--logfile=`** (ცარიელი).

### `runuser` და `sudo` PATH
`sudo`-ს `secure_path` არ შეიცავს `/usr/sbin`-ს → საჭიროა
**აბსოლუტური გზა** `/usr/sbin/runuser`. წინააღმდეგ შემთხვევაში exit 255
output-ის გარეშე.

---

## Deploy

```
კოდი → git push → სერვერზე ./deploy.sh → Odoo
```

**GitHub:** https://github.com/refreshg/fmg_flats_figurebi (`main`)

```bash
cd ~/vertikali-src && ./deploy.sh          # განახლება
cd ~/vertikali-src && ./deploy.sh install  # პირველი ინსტალაცია
```

### სერვერი
- Odoo 19.0+e, `odoo.service`, მომხმარებელი **`odoo`** (ერთი "d")
- SSH მომხმარებელი **`oddo`** (ორი "d") — **სხვადასხვა მომხმარებელია**
- SSH გასაღები: `~/.ssh/id_ed25519` ამ კომპიუტერზე (2026-08-23-ს ახალი
  დაგენერირდა — ძველი დაიკარგა; public key `oddo`-ს authorized_keys-შია)
- Odoo-ს ლოგი: `/var/log/odoo/odoo.log` — `oddo`-ს ეკითხება; დეპლოის
  ჩავარდნისას რეალური traceback აქ არის (სერვერზე დაყენებული ძველი
  helper `/tmp/vk-deploy-last.log`-ს ცარიელს ტოვებს)
- SSH მხოლოდ **შიდა IP-ზე** `192.168.100.71:22` (VPN). გარე `46.233.53.183`-ზე
  პორტი 22 დახურულია; `2223` = Odoo
- addons: `/opt/odoo/custom-addons/vertikali`
- venv: `/opt/odoo/odoo-19/venv/bin/python` (`oddo`-სთვის არ იკითხება —
  ვალიდაციისთვის გამოიყენე `python3`)

### `vk-deploy-root`
root-ის სკრიპტი ფიქსირებული გზებით, NOPASSWD sudo მხოლოდ **მასზე**.
`cp`/`rm`/`chown`-ის დაშვება სრული root-ის ტოლფასი იქნებოდა.

⚠️ **მისი შეცვლა ხელით reinstall-ს მოითხოვს:**
```bash
sudo install -o root -g root -m 755 ~/vertikali-src/vk-deploy-root.sh /usr/local/sbin/vk-deploy-root
```
ეს შეგნებულია: root-ის სკრიპტი user-ის ფოლდერიდან რომ იკითხავდეს თავს —
privilege escalation იქნებოდა. `deploy.sh` გაფრთხილებთ, თუ ძველია.

`vk-deploy-root doctor` — read-only დიაგნოსტიკა.

---

## წესები

- პასუხები **ქართულად**, კოდის კომენტარები **ინგლისურად**
- ცვლილება ჯერ ახსენი, მერე დაწერე
- ერთ ჯერზე ერთი ამოცანა
- ველების პრეფიქსი `vk_`
- ახალ მოდელს **აუცილებლად** სჭირდება `ir.model.access.csv` ჩანაწერი
- API-ით გადაამოწმე, სანამ ივარაუდებ — მეხსიერებით ნუ იმუშავებ

---

## შემდეგი ნაბიჯები

_(მდგომარეობა 2026-08-24, მოდული v19.0.3.5.3)_

### 1. სართულის ზედხედები და ზონები (მიმდინარე)
21 ჩანაწერი შექმნილია, ფასადს მიბმული; გეგმის სურათები ნაწილობრივ აკლია.
- ატვირთეთ თითო სართულის ზედხედი (Configuration → Visual Views);
  **Copy plan…** ვიზარდი გეგმას + ზონებს ერთდროულად ავრცელებს სართულებზე
- მიაბით `product_tmpl_id` თითო ზონას — ზონაზე კლიკი ამის მერე ხსნის
  ბარათს; დემო ზონები მე-2 და მე-4 სართულებზეა

### 2. მონაცემების შევსება (UI-დან, კოდი არ სჭირდება)
- **View (ხედი)** მნიშვნელობები — `vertikali.option` ცარიელია view-ზე;
  ბინის ფორმის dropdown-იდან ან Configuration → Unit Attributes
- Condition — მხოლოდ 7 ბინაზეა შევსებული
- Layout Type — ყველა 126-ს Type 1 აქვს (API-ით მასობრივად მიენიჭა);
  რეალურად ტიპები უნდა დაზუსტდეს და თითოს თავისი გეგმა აეტვირთოს
- **A-2203 sold-ია** სატესტო S00005-ით („customer 1") — გასათავისუფლებლად
  უბრალოდ გააუქმე ორდერი: ნაშთი თავისით დაბრუნდება (return-on-cancel)

### 3. CRM flow — ✅ დანერგილია (2026-08-24, v19.0.3.0.0)
- `crm.lead.vk_unit_ids` + ღილაკები: Choose Units (pick-რეჟიმი),
  Show on Plan (focus-კონტურები), Reserve (ორდერი + ნაშთის რეზერვი)
- სრული ციკლი **გატესტილია API-ით**: რეზერვაცია → კონტრაქტი → გაყიდვა
  (ნაშთი 0) → და ცალკე გაუქმება (ნაშთი დაბრუნდა); ორმაგი რეზერვაცია
  UserError-ს აგდებს; draft აღარ აკავებს. სატესტო მონაცემები გასუფთავდა.
- დარჩა: ლიდის ქცევის ქულა (ნახვები) — თუ დასჭირდა

### 4. კონფიგურაცია (UI-დან)
- **ვალუტა: USD → GEL** (ამჟამად USD-ია)
- კომპანიის სახელი "My Company" → რეალური

### 5. გასარკვევი
- ჯავშნის ვადა და ავტომატური გათავისუფლება? (`base_automation`
  დაინსტალირებულია, არ გამოგვიყენებია)
- ერთი ბინა რამდენიმე შეთავაზებაზე — დაშვებულია?
- Dynamic PDF — ბინის ბროშურა ბარათიდან (PRD roadmap #5)
- სერვერზე დაყენებული `vk-deploy-root` ძველია — განახლება:
  `sudo install -o root -g root -m 755 ~/vertikali-src/vk-deploy-root.sh /usr/local/sbin/vk-deploy-root`
